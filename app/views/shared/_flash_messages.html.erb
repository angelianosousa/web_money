<% flash.each do |flash_type, messages| %>
  <% flash_type = 'success' if flash_type == 'alert' %>

  <%= javascript_tag do %>
    showFlashNotification("<%= flash_type %>", "<%= messages %>");
  <% end %>
<% end %>
