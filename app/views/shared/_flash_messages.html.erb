<% type_messages = { 'notice'=>'success', 'alert'=>'danger'} %>

<% flash.each do |flash_type, message| %>
  <%= javascript_tag do %>
    showFlashNotification("<%= type_messages[flash_type] %>", "<%= message %>");
  <% end %>
<% end %>
