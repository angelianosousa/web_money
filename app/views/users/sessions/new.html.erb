<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

  <div class="form-group">
    <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Email", class:"form-control" %>
  </div>

  <div class="form-group">
    <%= f.password_field :password, autocomplete: "current-password", placeholder: "Sua senha", class:"form-control" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me, class:"form-control-md" %>
      <%= f.label :Lembrar_de_mim %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Entrar", class:"btn btn-primary btn-block", style:"float:none;margin-left:0;" %>
  </div>
<% end %>

<%= render "users/shared/links" %>
